# Build output (root-anchored so cmd/cloudcmder/ directory is not excluded)
/cloudcmder
/cloudcmder_ci
dist/

# SQLite assessment databases — these are operator artefacts, not source
*.db
*.db-shm
*.db-wal

# macOS
.DS_Store

# Go
*.test
*.out
vendor/

# Worktrees
.worktrees/
.claude/worktrees/

# Editor
.idea/
.vscode/
*.swp
*.swo

# Release artefacts (goreleaser --snapshot output at repo root)
/checksums.txt
/cloudcmder_*.tar.gz

# Website distribution staging (serve via GitHub Releases, not git)
/website/downloads/

# Operator scan outputs that land in the workdir
/inventory.xlsx
/docs/sku_billing_Reports*.xlsx

